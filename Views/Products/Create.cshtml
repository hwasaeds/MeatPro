@model MeatPro.ViewModels.ProductFormViewModel
@{
    ViewData["Title"] = "New Product";
}

<div class="module-card">
    <div class="section-title align-items-start flex-column flex-md-row">
        <div>
            <div class="section-kicker text-dark">Products</div>
            <h1 class="h3 mb-1">Create Product</h1>
            <p class="text-secondary mb-0">Define a new product for production planning and inventory tracking.</p>
        </div>
        <a asp-action="Index" class="btn btn-outline-meatpro"><i class="bi bi-arrow-left me-1"></i>Back to list</a>
    </div>

    <form asp-action="Create" method="post" class="vstack gap-4">
        <partial name="_Form" model="Model" />
        <div class="d-flex justify-content-end gap-2">
            <a asp-action="Index" class="btn btn-outline-meatpro"><i class="bi bi-x-lg me-1"></i>Cancel</a>
            <button type="submit" class="btn btn-meatpro"><i class="bi bi-check-lg me-1"></i>Save Product</button>
        </div>
    </form>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
